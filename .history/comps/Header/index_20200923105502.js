import React from 'react';
import styled from 'styled-components';

const HeadTitle = styled.h1`
    font-size: 18px;
    color: blue;
`;

const Header = ({}) => {
    return <HeadTitle>Ke-Ju</HeadTitle>;
}

export default Header;